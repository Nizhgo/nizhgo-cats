import styled from "styled-components";

const Card = styled.div`
    background: rgba(255, 255, 255, 0.95);
    border-radius: 20px;
    color: black;
    margin-inline: auto;`
;

export default Card;